# FloodSafe LK frontend

React + Vite client for Flood Incident Management. See the [root README](../README.md) for API setup, CORS, and the CRUD checklist.

```powershell
npm install
npm run dev
```

Requires the API at `VITE_API_BASE_URL` (default `http://localhost:5203`).
